{
  "name": "assets",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "watch": "vite build --watch",
    "generate": "openapi-generator-cli generate -g typescript-fetch -i http://localhost:3268/umbraco/swagger/sustainability/swagger.json?urls.primaryName=Sustainability%20API -o src/api --skip-validate-spec"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.9.0",
    "@umbraco-cms/backoffice": "^14.0.0--preview004",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
